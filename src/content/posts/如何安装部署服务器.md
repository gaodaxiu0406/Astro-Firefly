---
# 必填。项目名称。
title: "如何安装部署服务器"
# 可选，和文章一样使用。
slug: 如何安装部署服务器
# 必填。发布/更新日期，如 2025-10-01。用于排序（配合 order）。
published: 2017-07-31
# 可选，默认 false。设为 true 时生产构建会隐藏该页，预览可见。
draft: false
# 可选。手动排序权重，越大越靠前；未设置则按 published 降序。
order: 1
# 可选。卡片简介 + 详情页描述。
description: "如何安装部署服务器"
# 可选。封面图。支持完整 URL、公共根路径（/images/xxx.png）、相对路径（相对本文件目录，如 images/xxx.png）。留空则不显示封面。
# image: "./images/MongoDB安装目录.png"
# 可选。项目状态，用标准 key:planning计划中 developing开发中 published已发布 archived已归档
status: "archived"
# 分类
category: 服务器
tags:
  - 服务器
# 可选。外链按钮数组：{ label, icon, value }。icon 可用 astro-icon 名（如 fa7-brands:github）、图片 URL，或留空用 label 首字母。
# link: []
# 可选。页面语言，如 zh_CN。
# lang: zh_CN
# 可选。标签，列表页与详情页显示为 #标签。
---
## 购买机器`https://www.aliyun.com/`
<!-- more -->
```js
116.62.9.168
用户名:root
密码:Zfpx2017
```

## 连接服务器
```js
ssh root@116.62.9.168
```

## 更新操作系统
```js
apt-get update
```

## 安装 npm
```js
apt-get install npm
```

## 安装node
```js
apt-get install npm
```
## 安装node版本管理器n n是node的管理器 可以通过他管理node的版本
```js
npm install n -g
```
## 安装node7.5版本
```js
n 7.5
```
- 出现`node-v7.5.0`就安装成功了

## 安装mongodb
```js
apt-get install mongodb
```
## 安装git
```js
apt-get install git
```

## 克隆爬虫的地址
```js
git clone https://github.com/zhufengnodejs/201704crawl.git
```

## 进入目录201704crawl
```js
cd 201704crawl
```

## 安装依赖
```js
  npm install
```

## 启动server.js
```js
node server.js
```

## 执行脚本main.js(先进入main.js所在的文件夹)
- 1.进入main.js所在的文件夹执行main.js
```js
cd tasks
node main.js
```
- 2.ctrl+c结束掉之前的任务,再启动server.js
```js
node server.js
```

## 访问(格式-->ip:端口号)
- 在浏览器输入地址`116.62.9.168:8080`访问
    - `http://116.62.9.168:8080/`
- 此时还是静态网页 别人无法访问

## 在电脑上安装程序`Xftp 5`

## 安装nginx
- Nginx是一个高性能的HTTP和反向代理服务器
```js
apt-get install nginx
```
- 此时就可以上传自己的项目,访问自己的服务器
- 但是一旦窗口关掉,就无法访问

### nginx命令
- 启动 nginx nginx -c /etc/nginx/nginx.conf
- 关闭 nginx nginx -s stop
- 重读配置文件 nginx -s reload kill -HUP nginx
- 常用命令 service nginx {start|stop|status|restart|reload|configtest|}

### 配置nginx反向代理和负载均衡
```css
upstream crawl{
    ip_hash;
    server 127.0.0.1:3000 weight=10;
    server 127.0.0.1:4000 weight=1;
}

server {
        listen 80;
        server_name www.gdxiu.cn;

        location / {
           proxy_pass http://crawl;
        }
}
```

## 安装pm2
- 强大的进程管理器 进程异常退出时pm2会尝试重启
```js
npm install pm2 -g
```

- 进入201704crawl文件夹,在根目录下执行以下命令
```js
cd /root
cd 201704crawl
```

- 切换到server.js文件所在目录,启动pm2应用
```js
pm2 start server.js --name "crawl"
//即pm2 start server.js --name '自己取名字'
```

## 如何停止pm2上的项目
### 1.查看node进程号
```js
 ps -ef | grep node
```

### 2.停止
```js
pm2 stop crawl
```

### 3.杀死进程
```js
kill 8796(进程号)
```

## 复活:如何重新建立连接
```js
pm2 start server.js --name "crawl"
```

## 启动
```js
pm2 start crawl
```

## pm2常用命令
- 用pm2启动node

|命令|用途|
|------|------|
|pm2 start app.js --name "crawl"|启动应用|
|pm2 list|查看所有应用|
|pm2 restart crawl|重启应用|
|pm2 stop crawl|停止应用|
|pm2 delete crawl|删除应用|

### 关于scp命令
- 网址`http://www.cnblogs.com/hitwtx/archive/2011/11/16/2251254.html `

cat 查看文件内容

## vi命令
```js
vi crawl
1000dd//删除1000行  vi的一个命令
```

## 重启服务
```js
nginx -s reload
```

## 推荐视频地址
```js
http://www.ablesky.com/kecheng/detail_926049
```
